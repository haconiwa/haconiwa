sudo: false
language: c
addons:
  apt:
    packages:
      - bison
      - git
      - gperf
      - cgroup-lite
      - libpam0g-dev
      - autotools-dev
      - libcap-dev
      - libargtable2-dev
compiler:
  - gcc
  - clang
env:
  - MRUBY_VERSION=""
  - MRUBY_VERSION=master
branches:
  only:
    - master

script:
  - rake test
